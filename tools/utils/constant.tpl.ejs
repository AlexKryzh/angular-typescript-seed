export class Constants{
    <% constants.forEach(function(constant) { %>
        <%- constant.name %>: any;
    <% }) %>
    constructor(){
    <% constants.forEach(function(constant) { %>
        this.<%- constant.name %>= <%= constant.value %>;
    <% }) %>
    }
}